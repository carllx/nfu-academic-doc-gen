# How To: 2D

**Difficulty**: Advanced
**Estimated Time**: 15 minutes
**Tags**: workflow, integration

## Overview

Workflow: test 2d

## Prerequisites

**Required Modules:**
- `pytest`
- `numpy`
- `numpy.lib._index_tricks_impl`
- `numpy.testing`
- `numpy`


## Step-by-Step Guide

### Step 1: Assign b = np.random.rand(...)

```python
b = np.random.rand(5, 5)
```

**Verification:**
```python
assert_(d.shape == (5, 10))
```

### Step 2: Assign c = np.random.rand(...)

```python
c = np.random.rand(5, 5)
```

**Verification:**
```python
assert_array_equal(d[:, :5], b)
```

### Step 3: Assign d = value

```python
d = r_['1', b, c]
```

**Verification:**
```python
assert_array_equal(d[:, 5:], c)
```

### Step 4: Call assert_()

```python
assert_(d.shape == (5, 10))
```

**Verification:**
```python
assert_(d.shape == (10, 5))
```

### Step 5: Call assert_array_equal()

```python
assert_array_equal(d[:, :5], b)
```

**Verification:**
```python
assert_array_equal(d[:5, :], b)
```

### Step 6: Call assert_array_equal()

```python
assert_array_equal(d[:, 5:], c)
```

**Verification:**
```python
assert_array_equal(d[5:, :], c)
```

### Step 7: Assign d = value

```python
d = r_[b, c]
```

### Step 8: Call assert_()

```python
assert_(d.shape == (10, 5))
```

### Step 9: Call assert_array_equal()

```python
assert_array_equal(d[:5, :], b)
```

### Step 10: Call assert_array_equal()

```python
assert_array_equal(d[5:, :], c)
```


## Complete Example

```python
# Workflow
b = np.random.rand(5, 5)
c = np.random.rand(5, 5)
d = r_['1', b, c]
assert_(d.shape == (5, 10))
assert_array_equal(d[:, :5], b)
assert_array_equal(d[:, 5:], c)
d = r_[b, c]
assert_(d.shape == (10, 5))
assert_array_equal(d[:5, :], b)
assert_array_equal(d[5:, :], c)
```

## Next Steps


---

*Source: test_index_tricks.py:348 | Complexity: Advanced | Last updated: 2026-06-02*