# How To: G3

**Difficulty**: Intermediate
**Estimated Time**: 10 minutes
**Tags**: workflow, integration

## Overview

Workflow: Weighted betweenness centrality: G3

## Prerequisites

**Required Modules:**
- `math`
- `pytest`
- `networkx`


## Step-by-Step Guide

### Step 1: 'Weighted betweenness centrality: G3'

```python
'Weighted betweenness centrality: G3'
```

**Verification:**
```python
assert b[n] == pytest.approx(b_answer[n], abs=1e-07)
```

### Step 2: Assign G = nx.MultiGraph(...)

```python
G = nx.MultiGraph(weighted_G())
```

### Step 3: Assign es = value

```python
es = list(G.edges(data=True))[::2]
```

### Step 4: Call G.add_edges_from()

```python
G.add_edges_from(es)
```

### Step 5: Assign b_answer = value

```python
b_answer = {0: 2.0, 1: 0.0, 2: 4.0, 3: 3.0, 4: 4.0, 5: 0.0}
```

### Step 6: Assign b = nx.betweenness_centrality(...)

```python
b = nx.betweenness_centrality(G, weight='weight', normalized=False)
```

**Verification:**
```python
assert b[n] == pytest.approx(b_answer[n], abs=1e-07)
```


## Complete Example

```python
# Workflow
'Weighted betweenness centrality: G3'
G = nx.MultiGraph(weighted_G())
es = list(G.edges(data=True))[::2]
G.add_edges_from(es)
b_answer = {0: 2.0, 1: 0.0, 2: 4.0, 3: 3.0, 4: 4.0, 5: 0.0}
b = nx.betweenness_centrality(G, weight='weight', normalized=False)
for n in sorted(G):
    assert b[n] == pytest.approx(b_answer[n], abs=1e-07)
```

## Next Steps


---

*Source: test_betweenness_centrality.py:641 | Complexity: Intermediate | Last updated: 2026-06-02*