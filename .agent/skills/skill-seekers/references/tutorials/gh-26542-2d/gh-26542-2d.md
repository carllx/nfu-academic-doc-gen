# How To: Gh 26542 2D

**Difficulty**: Intermediate
**Estimated Time**: 10 minutes
**Tags**: workflow, integration

## Overview

Workflow: test gh 26542 2d

## Prerequisites

**Required Modules:**
- `functools`
- `inspect`
- `operator`
- `sys`
- `warnings`
- `itertools`
- `pytest`
- `numpy`
- `numpy._core._multiarray_tests`
- `numpy.exceptions`
- `numpy.testing`


## Step-by-Step Guide

### Step 1: Assign a = np.array(...)

```python
a = np.array([[0, 1, 2]])
```

**Verification:**
```python
assert_equal(a, expected)
```

### Step 2: Assign idx_row = np.zeros(...)

```python
idx_row = np.zeros(3, dtype=int)
```

### Step 3: Assign idx_col = np.array(...)

```python
idx_col = np.array([2, 1, 0])
```

### Step 4: Assign unknown = a

```python
a[idx_row, idx_col] = a
```

### Step 5: Assign expected = np.array(...)

```python
expected = np.array([[2, 1, 0]])
```

### Step 6: Call assert_equal()

```python
assert_equal(a, expected)
```


## Complete Example

```python
# Workflow
a = np.array([[0, 1, 2]])
idx_row = np.zeros(3, dtype=int)
idx_col = np.array([2, 1, 0])
a[idx_row, idx_col] = a
expected = np.array([[2, 1, 0]])
assert_equal(a, expected)
```

## Next Steps


---

*Source: test_indexing.py:149 | Complexity: Intermediate | Last updated: 2026-06-02*