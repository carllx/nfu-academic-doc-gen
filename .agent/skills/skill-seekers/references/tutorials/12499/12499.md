# How To: 12499

**Difficulty**: Advanced
**Estimated Time**: 20 minutes
**Tags**: pytest, workflow, integration

## Overview

Workflow: test 12499

## Prerequisites

**Required Modules:**
- `numpy`
- `pytest`
- `pandas`
- `pandas`
- `pandas._testing`


## Step-by-Step Guide

### Step 1: Assign ts = Timestamp(...)

```python
ts = Timestamp('2016-03-01 03:13:22.98986', tz='UTC')
```

### Step 2: Assign data = value

```python
data = [{'one': 0, 'two': ts}]
```

### Step 3: Assign orig = DataFrame(...)

```python
orig = DataFrame(data)
```

### Step 4: Assign df = orig.copy(...)

```python
df = orig.copy()
```

### Step 5: Assign unknown = value

```python
df.loc[1] = [np.nan, NaT]
```

### Step 6: Assign expected = DataFrame(...)

```python
expected = DataFrame({'one': [0, np.nan], 'two': Series([ts, NaT], dtype='datetime64[ns, UTC]')})
```

### Step 7: Call tm.assert_frame_equal()

```python
tm.assert_frame_equal(df, expected)
```

### Step 8: Assign data = value

```python
data = [{'one': 0, 'two': ts}]
```

### Step 9: Assign df = orig.copy(...)

```python
df = orig.copy()
```

### Step 10: Assign unknown = value

```python
df.loc[1, :] = [np.nan, NaT]
```

### Step 11: Call tm.assert_frame_equal()

```python
tm.assert_frame_equal(df, expected)
```


## Complete Example

```python
# Workflow
ts = Timestamp('2016-03-01 03:13:22.98986', tz='UTC')
data = [{'one': 0, 'two': ts}]
orig = DataFrame(data)
df = orig.copy()
df.loc[1] = [np.nan, NaT]
expected = DataFrame({'one': [0, np.nan], 'two': Series([ts, NaT], dtype='datetime64[ns, UTC]')})
tm.assert_frame_equal(df, expected)
data = [{'one': 0, 'two': ts}]
df = orig.copy()
df.loc[1, :] = [np.nan, NaT]
tm.assert_frame_equal(df, expected)
```

## Next Steps


---

*Source: test_coercion.py:165 | Complexity: Advanced | Last updated: 2026-06-02*