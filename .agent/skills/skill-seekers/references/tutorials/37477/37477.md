# How To: 37477

**Difficulty**: Advanced
**Estimated Time**: 20 minutes
**Tags**: workflow, integration

## Overview

Workflow: test 37477

## Prerequisites

**Required Modules:**
- `numpy`
- `pytest`
- `pandas`
- `pandas`
- `pandas._testing`


## Step-by-Step Guide

### Step 1: Assign orig = DataFrame(...)

```python
orig = DataFrame({'A': [1, 2, 3], 'B': [3, 4, 5]})
```

### Step 2: Assign expected = DataFrame(...)

```python
expected = DataFrame({'A': [1, 2, 3], 'B': [3, 1.2, 5]})
```

### Step 3: Assign df = orig.copy(...)

```python
df = orig.copy()
```

### Step 4: Call tm.assert_frame_equal()

```python
tm.assert_frame_equal(df, expected)
```

### Step 5: Assign df = orig.copy(...)

```python
df = orig.copy()
```

### Step 6: Call tm.assert_frame_equal()

```python
tm.assert_frame_equal(df, expected)
```

### Step 7: Assign df = orig.copy(...)

```python
df = orig.copy()
```

### Step 8: Call tm.assert_frame_equal()

```python
tm.assert_frame_equal(df, expected)
```

### Step 9: Assign df = orig.copy(...)

```python
df = orig.copy()
```

### Step 10: Call tm.assert_frame_equal()

```python
tm.assert_frame_equal(df, expected)
```

### Step 11: Assign unknown = 1.2

```python
df.at[1, 'B'] = 1.2
```

### Step 12: Assign unknown = 1.2

```python
df.loc[1, 'B'] = 1.2
```

### Step 13: Assign unknown = 1.2

```python
df.iat[1, 1] = 1.2
```

### Step 14: Assign unknown = 1.2

```python
df.iloc[1, 1] = 1.2
```


## Complete Example

```python
# Workflow
orig = DataFrame({'A': [1, 2, 3], 'B': [3, 4, 5]})
expected = DataFrame({'A': [1, 2, 3], 'B': [3, 1.2, 5]})
df = orig.copy()
with tm.assert_produces_warning(FutureWarning, match='Setting an item of incompatible dtype'):
    df.at[1, 'B'] = 1.2
tm.assert_frame_equal(df, expected)
df = orig.copy()
with tm.assert_produces_warning(FutureWarning, match='Setting an item of incompatible dtype'):
    df.loc[1, 'B'] = 1.2
tm.assert_frame_equal(df, expected)
df = orig.copy()
with tm.assert_produces_warning(FutureWarning, match='Setting an item of incompatible dtype'):
    df.iat[1, 1] = 1.2
tm.assert_frame_equal(df, expected)
df = orig.copy()
with tm.assert_produces_warning(FutureWarning, match='Setting an item of incompatible dtype'):
    df.iloc[1, 1] = 1.2
tm.assert_frame_equal(df, expected)
```

## Next Steps


---

*Source: test_coercion.py:48 | Complexity: Advanced | Last updated: 2026-06-02*