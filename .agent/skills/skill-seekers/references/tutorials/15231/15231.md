# How To: 15231

**Difficulty**: Intermediate
**Estimated Time**: 10 minutes
**Tags**: pytest, workflow, integration

## Overview

Workflow: test 15231

## Prerequisites

**Required Modules:**
- `numpy`
- `pytest`
- `pandas`
- `pandas`
- `pandas._testing`


## Step-by-Step Guide

### Step 1: Assign df = DataFrame(...)

```python
df = DataFrame([[1, 2], [3, 4]], columns=['a', 'b'])
```

**Verification:**
```python
assert (df.dtypes == np.int64).all()
```

### Step 2: Assign unknown = Series(...)

```python
df.loc[2] = Series({'a': 5, 'b': 6})
```

**Verification:**
```python
assert (df.dtypes == np.int64).all()
```

### Step 3: Assign unknown = Series(...)

```python
df.loc[3] = Series({'a': 7})
```

### Step 4: Assign exp_dtypes = Series(...)

```python
exp_dtypes = Series([np.int64, np.float64], dtype=object, index=['a', 'b'])
```

### Step 5: Call tm.assert_series_equal()

```python
tm.assert_series_equal(df.dtypes, exp_dtypes)
```


## Complete Example

```python
# Workflow
df = DataFrame([[1, 2], [3, 4]], columns=['a', 'b'])
df.loc[2] = Series({'a': 5, 'b': 6})
assert (df.dtypes == np.int64).all()
df.loc[3] = Series({'a': 7})
exp_dtypes = Series([np.int64, np.float64], dtype=object, index=['a', 'b'])
tm.assert_series_equal(df.dtypes, exp_dtypes)
```

## Next Steps


---

*Source: test_coercion.py:135 | Complexity: Intermediate | Last updated: 2026-06-02*