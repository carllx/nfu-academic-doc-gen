# How To: 1000 Sep

**Difficulty**: Intermediate
**Estimated Time**: 15 minutes
**Tags**: workflow, integration

## Overview

Workflow: test 1000 sep

## Prerequisites

- [ ] Setup code must be executed first

**Required Modules:**
- `datetime`
- `inspect`
- `io`
- `os`
- `pathlib`
- `sys`
- `numpy`
- `pytest`
- `pandas._config`
- `pandas.compat`
- `pandas.errors`
- `pandas`
- `pandas._testing`
- `pandas.io.parsers`
- `pandas.io.parsers.c_parser_wrapper`

**Setup Required:**
```python
# Fixtures: all_parsers
```

## Step-by-Step Guide

### Step 1: Assign parser = all_parsers

```python
parser = all_parsers
```

### Step 2: Assign data = 'A|B|C\n1|2,334|5\n10|13|10.\n'

```python
data = 'A|B|C\n1|2,334|5\n10|13|10.\n'
```

### Step 3: Assign expected = DataFrame(...)

```python
expected = DataFrame({'A': [1, 10], 'B': [2334, 13], 'C': [5, 10.0]})
```

### Step 4: Assign result = parser.read_csv(...)

```python
result = parser.read_csv(StringIO(data), sep='|', thousands=',')
```

### Step 5: Call tm.assert_frame_equal()

```python
tm.assert_frame_equal(result, expected)
```

### Step 6: Assign msg = "The 'thousands' option is not supported with the 'pyarrow' engine"

```python
msg = "The 'thousands' option is not supported with the 'pyarrow' engine"
```

### Step 7: Call parser.read_csv()

```python
parser.read_csv(StringIO(data), sep='|', thousands=',')
```


## Complete Example

```python
# Setup
# Fixtures: all_parsers

# Workflow
parser = all_parsers
data = 'A|B|C\n1|2,334|5\n10|13|10.\n'
expected = DataFrame({'A': [1, 10], 'B': [2334, 13], 'C': [5, 10.0]})
if parser.engine == 'pyarrow':
    msg = "The 'thousands' option is not supported with the 'pyarrow' engine"
    with pytest.raises(ValueError, match=msg):
        parser.read_csv(StringIO(data), sep='|', thousands=',')
    return
result = parser.read_csv(StringIO(data), sep='|', thousands=',')
tm.assert_frame_equal(result, expected)
```

## Next Steps


---

*Source: test_common_basic.py:126 | Complexity: Intermediate | Last updated: 2026-06-02*