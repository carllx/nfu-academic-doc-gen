# How To: Gh 21022

**Difficulty**: Intermediate
**Estimated Time**: 15 minutes
**Tags**: workflow, integration

## Overview

Workflow: test gh 21022

## Prerequisites

**Required Modules:**
- `copy`
- `inspect`
- `itertools`
- `operator`
- `pickle`
- `sys`
- `textwrap`
- `warnings`
- `functools`
- `pytest`
- `numpy`
- `numpy._core.fromnumeric`
- `numpy._core.umath`
- `numpy.ma.core`
- `numpy`
- `numpy._utils`
- `numpy.exceptions`
- `numpy.ma.core`
- `numpy.ma.testutils`
- `numpy.testing`
- `numpy.testing._private.utils`
- `datetime`
- `copy`
- `io`
- `copy`
- `copy`


## Step-by-Step Guide

### Step 1: Assign source = np.ma.masked_array(...)

```python
source = np.ma.masked_array(data=[-1, -1], mask=True, dtype=np.float64)
```

### Step 2: Assign axis = np.array(...)

```python
axis = np.array(0)
```

### Step 3: Assign result = np.prod(...)

```python
result = np.prod(source, axis=axis, keepdims=False)
```

### Step 4: Assign result = np.ma.masked_array(...)

```python
result = np.ma.masked_array(result, mask=np.ones(result.shape, dtype=np.bool))
```

### Step 5: Assign array = np.ma.masked_array(...)

```python
array = np.ma.masked_array(data=-1, mask=True, dtype=np.float64)
```

### Step 6: Call copy.deepcopy()

```python
copy.deepcopy(array)
```

### Step 7: Call copy.deepcopy()

```python
copy.deepcopy(result)
```


## Complete Example

```python
# Workflow
source = np.ma.masked_array(data=[-1, -1], mask=True, dtype=np.float64)
axis = np.array(0)
result = np.prod(source, axis=axis, keepdims=False)
result = np.ma.masked_array(result, mask=np.ones(result.shape, dtype=np.bool))
array = np.ma.masked_array(data=-1, mask=True, dtype=np.float64)
copy.deepcopy(array)
copy.deepcopy(result)
```

## Next Steps


---

*Source: test_core.py:5920 | Complexity: Intermediate | Last updated: 2026-06-02*