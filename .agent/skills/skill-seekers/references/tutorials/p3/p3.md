# How To: P3

**Difficulty**: Intermediate
**Estimated Time**: 10 minutes
**Tags**: workflow, integration

## Overview

Workflow: Eigenvector centrality: P3

## Prerequisites

**Required Modules:**
- `math`
- `pytest`
- `networkx`


## Step-by-Step Guide

### Step 1: 'Eigenvector centrality: P3'

```python
'Eigenvector centrality: P3'
```

**Verification:**
```python
assert b[n] == pytest.approx(b_answer[n], abs=0.0001)
```

### Step 2: Assign G = nx.path_graph(...)

```python
G = nx.path_graph(3)
```

**Verification:**
```python
assert b[n] == pytest.approx(b_answer[n], abs=0.0001)
```

### Step 3: Assign b_answer = value

```python
b_answer = {0: 0.5, 1: 0.7071, 2: 0.5}
```

### Step 4: Assign b = nx.eigenvector_centrality_numpy(...)

```python
b = nx.eigenvector_centrality_numpy(G)
```

### Step 5: Assign b = nx.eigenvector_centrality(...)

```python
b = nx.eigenvector_centrality(G)
```

**Verification:**
```python
assert b[n] == pytest.approx(b_answer[n], abs=0.0001)
```


## Complete Example

```python
# Workflow
'Eigenvector centrality: P3'
G = nx.path_graph(3)
b_answer = {0: 0.5, 1: 0.7071, 2: 0.5}
b = nx.eigenvector_centrality_numpy(G)
for n in sorted(G):
    assert b[n] == pytest.approx(b_answer[n], abs=0.0001)
b = nx.eigenvector_centrality(G)
for n in sorted(G):
    assert b[n] == pytest.approx(b_answer[n], abs=0.0001)
```

## Next Steps


---

*Source: test_eigenvector_centrality.py:29 | Complexity: Intermediate | Last updated: 2026-06-02*