# How To: K5

**Difficulty**: Advanced
**Estimated Time**: 15 minutes
**Tags**: workflow, integration

## Overview

Workflow: Eigenvector centrality: K5

## Prerequisites

**Required Modules:**
- `math`
- `pytest`
- `networkx`


## Step-by-Step Guide

### Step 1: 'Eigenvector centrality: K5'

```python
'Eigenvector centrality: K5'
```

**Verification:**
```python
assert b[n] == pytest.approx(b_answer[n], abs=1e-07)
```

### Step 2: Assign G = nx.complete_graph(...)

```python
G = nx.complete_graph(5)
```

**Verification:**
```python
assert b[n] == pytest.approx(b_answer[n], abs=1e-07)
```

### Step 3: Assign b = nx.eigenvector_centrality(...)

```python
b = nx.eigenvector_centrality(G)
```

**Verification:**
```python
assert b[n] == pytest.approx(b_answer[n], abs=0.001)
```

### Step 4: Assign v = math.sqrt(...)

```python
v = math.sqrt(1 / 5.0)
```

### Step 5: Assign b_answer = dict.fromkeys(...)

```python
b_answer = dict.fromkeys(G, v)
```

### Step 6: Assign nstart = value

```python
nstart = {n: 1 for n in G}
```

### Step 7: Assign b = nx.eigenvector_centrality(...)

```python
b = nx.eigenvector_centrality(G, nstart=nstart)
```

### Step 8: Assign b = nx.eigenvector_centrality_numpy(...)

```python
b = nx.eigenvector_centrality_numpy(G)
```

**Verification:**
```python
assert b[n] == pytest.approx(b_answer[n], abs=1e-07)
```


## Complete Example

```python
# Workflow
'Eigenvector centrality: K5'
G = nx.complete_graph(5)
b = nx.eigenvector_centrality(G)
v = math.sqrt(1 / 5.0)
b_answer = dict.fromkeys(G, v)
for n in sorted(G):
    assert b[n] == pytest.approx(b_answer[n], abs=1e-07)
nstart = {n: 1 for n in G}
b = nx.eigenvector_centrality(G, nstart=nstart)
for n in sorted(G):
    assert b[n] == pytest.approx(b_answer[n], abs=1e-07)
b = nx.eigenvector_centrality_numpy(G)
for n in sorted(G):
    assert b[n] == pytest.approx(b_answer[n], abs=0.001)
```

## Next Steps


---

*Source: test_eigenvector_centrality.py:12 | Complexity: Advanced | Last updated: 2026-06-02*