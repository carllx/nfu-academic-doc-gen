# How To: Node Attribute Xy Undirected

**Difficulty**: Beginner
**Estimated Time**: 5 minutes

## Overview

Instantiate sorted: test node attribute xy undirected

## Prerequisites

**Required Modules:**
- `networkx`
- `base_test`


## Step-by-Step Guide

### Step 1: Assign attrxy_result = sorted(...)

```python
attrxy_result = sorted([('one', 'one'), ('one', 'one'), ('two', 'two'), ('two', 'two'), ('one', 'red'), ('red', 'one'), ('blue', 'two'), ('two', 'blue')])
```


## Complete Example

```python
# Workflow
attrxy_result = sorted([('one', 'one'), ('one', 'one'), ('two', 'two'), ('two', 'two'), ('one', 'red'), ('red', 'one'), ('blue', 'two'), ('two', 'blue')])
```

## Next Steps


---

*Source: test_pairs.py:9 | Complexity: Beginner | Last updated: 2026-06-02*