# How To: Gh 26542

**Difficulty**: Intermediate
**Estimated Time**: 10 minutes
**Tags**: workflow, integration

## Overview

Workflow: test gh 26542

## Prerequisites

**Required Modules:**
- `functools`
- `inspect`
- `operator`
- `sys`
- `warnings`
- `itertools`
- `pytest`
- `numpy`
- `numpy._core._multiarray_tests`
- `numpy.exceptions`
- `numpy.testing`


## Step-by-Step Guide

### Step 1: Assign a = np.array(...)

```python
a = np.array([0, 1, 2])
```

**Verification:**
```python
assert_equal(a, expected)
```

### Step 2: Assign idx = np.array(...)

```python
idx = np.array([2, 1, 0])
```

### Step 3: Assign unknown = a

```python
a[idx] = a
```

### Step 4: Assign expected = np.array(...)

```python
expected = np.array([2, 1, 0])
```

### Step 5: Call assert_equal()

```python
assert_equal(a, expected)
```


## Complete Example

```python
# Workflow
a = np.array([0, 1, 2])
idx = np.array([2, 1, 0])
a[idx] = a
expected = np.array([2, 1, 0])
assert_equal(a, expected)
```

## Next Steps


---

*Source: test_indexing.py:142 | Complexity: Intermediate | Last updated: 2026-06-02*